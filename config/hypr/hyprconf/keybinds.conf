###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier
$scripts = $HOME/.config/hypr/scripts/master
# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, F, togglefloating,
bind = $mainMod SHIFT, F, fullscreen,
bind = $mainMod, R, exec, $scripts/open_launcher.sh
bind = $mainMod, V, exec, $scripts/open_clipboard.sh
# bind = $mainMod, P, pseudo, # dwindle
# Submap: subsystem
bind = ALT, S, submap, system
submap = system, reset
bind = , B, exec, ghostty --class=com.ghostty.float -e bluetui
bind = , W, exec, ghostty --class=com.ghostty.float -e sh -c "wlctl || nmtui"
bind = , A, exec, ghostty --class=com.ghostty.float -e pulsemixer
bind = , M, exec, ghostty --class=com.ghostty.float -e btop
bind = , escape, submap, reset
submap = reset
# Submap: power
bind = ALT, P, submap, power
submap = power, reset
bind = , S, exec, poweroff
bind = , R, exec, reboot
bind = , L, exec, $scripts/lock_screen.sh
bind = , H, exec, $scripts/toggle_idle.sh
bind = , M, exit
bind = , escape, submap, reset
submap = reset
# Move focus with mainMod + vim keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9

# Relative workspcae movement 
bind = $mainMod, N, workspace, e+1 
bind = $mainMod, P, workspace, e-1 

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

bind = $mainMod, 0, togglespecialworkspace, zero
bind = $mainMod SHIFT, 0, movetoworkspace, special:zero
# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
# Custom
bind = $mainMod, b, exec, $scripts/toggle_bar.sh
# HyprShot
bind = $mainMod, Print, exec, hyprshot -m window -o ~/Pictures/HyprShots
bind = $mainMod SHIFT, Print, exec, hyprshot -m output -o ~/Pictures/HyprShots
bind = $mainMod CTRL, Print, exec, hyprshot -m region -o ~/Pictures/HyprShots
# media control with playerctl
bind = $mainMod, Home, exec, playerctl previous
bind = $mainMod, Insert, exec, playerctl --all-players play-pause
bind = $mainMod, End, exec, playerctl next
bind = $mainMod, Delete, exec, playerctl shuffle toggle && notify-send --transient --urgency=low "Shuffle $(playerctl shuffle)"

